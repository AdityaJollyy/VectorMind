# VectorMind

An open-source NotebookLM-style app: upload documents, web pages, or text and chat with them using Retrieval-Augmented Generation (RAG).

- **client/** — React + Vite frontend
- **server/** — Node.js + Express API

Setup and architecture docs coming as the project is built.
EOF